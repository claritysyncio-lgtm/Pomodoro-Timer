{
  "name": "pomodoro-timer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "terser": "^5.0.0"
  }
}
